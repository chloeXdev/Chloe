<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AURA | The Future of Wealth</title>

    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>

    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@200;300;400;500;700&family=Playfair+Display:ital,wght@0,400;0,600;1,400&display=swap" rel="stylesheet">

    <!-- Libraries -->
    <script src="https://unpkg.com/lucide@latest/dist/umd/lucide.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/ScrollTrigger.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@studio-freight/lenis@1.0.42/dist/lenis.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js"></script>

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        'aura-black': '#050505',
                        'aura-dark': '#0A0A0A',
                        'aura-gold': '#D4AF37',
                        'aura-gold-dim': '#8a7123',
                        'aura-gray': '#666666',
                        'aura-border': 'rgba(255,255,255,0.08)'
                    },
                    fontFamily: {
                        sans: ['Manrope', 'sans-serif'],
                        serif: ['Playfair Display', 'serif'],
                    },
                    animation: {
                        'spin-slow': 'spin 12s linear infinite',
                    }
                }
            }
        }
    </script>

    <style>
        body {
            background-color: #050505;
            color: #ffffff;
            overflow-x: hidden;
            -webkit-font-smoothing: antialiased;
            cursor: none; /* Hide default cursor */
        }

        /* Cinematic Noise Overlay */
        .noise-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 9000;
            opacity: 0.05;
            background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.65' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)'/%3E%3C/svg%3E");
        }

        /* Custom Cursor */
        .cursor-dot, .cursor-circle {
            position: fixed;
            top: 0;
            left: 0;
            transform: translate(-50%, -50%);
            border-radius: 50%;
            z-index: 9999;
            pointer-events: none;
        }
        .cursor-dot {
            width: 8px;
            height: 8px;
            background-color: #D4AF37;
        }
        .cursor-circle {
            width: 40px;
            height: 40px;
            border: 1px solid rgba(212, 175, 55, 0.5);
            transition: width 0.3s, height 0.3s, background-color 0.3s;
        }
        /* Cursor Hover State */
        body.hovering .cursor-circle {
            width: 60px;
            height: 60px;
            background-color: rgba(212, 175, 55, 0.1);
            border-color: #D4AF37;
        }

        /* Scroll Progress Bar */
        #scroll-progress {
            position: fixed;
            top: 0;
            left: 0;
            height: 2px;
            background: #D4AF37;
            width: 0%;
            z-index: 10000;
            transition: width 0.1s;
        }

        /* Preloader */
        #preloader {
            position: fixed;
            inset: 0;
            background: #000;
            z-index: 9999;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
        }
        .loader-bar {
            width: 200px;
            height: 1px;
            background: rgba(255,255,255,0.2);
            margin-top: 20px;
            position: relative;
            overflow: hidden;
        }
        .loader-progress {
            position: absolute;
            left: 0;
            top: 0;
            height: 100%;
            background: #D4AF37;
            width: 0%;
            transition: width 0.1s linear;
        }

        /* Typography */
        .text-huge {
            font-size: clamp(3rem, 11vw, 9rem);
            line-height: 0.9;
            letter-spacing: -0.04em;
        }
        .text-outline {
            -webkit-text-stroke: 1px rgba(255,255,255,0.1);
            color: transparent;
        }

        /* 3D Canvas */
        #webgl-canvas {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 0;
            opacity: 0.6;
        }

        /* Marquee */
        .marquee-wrap {
            overflow: hidden;
            white-space: nowrap;
            display: flex;
        }
        .marquee-item {
            display: flex;
            animation: marquee 30s linear infinite;
        }
        @keyframes marquee {
            0% { transform: translateX(0); }
            100% { transform: translateX(-100%); }
        }

        /* Page Transitions */
        .page-container {
            display: none;
            opacity: 0;
            transform: translateY(20px);
            transition: opacity 0.8s cubic-bezier(0.16, 1, 0.3, 1), transform 0.8s cubic-bezier(0.16, 1, 0.3, 1);
        }
        .page-container.active {
            display: block;
            opacity: 1;
            transform: translateY(0);
        }

        /* Header Glass */
        header.scrolled {
            background: rgba(5, 5, 5, 0.8);
            backdrop-filter: blur(20px);
            border-bottom: 1px solid rgba(255, 255, 255, 0.05);
        }

        /* Grid Background for CTA */
        .grid-bg {
            background-size: 50px 50px;
            background-image: linear-gradient(to right, rgba(255, 255, 255, 0.05) 1px, transparent 1px),
                              linear-gradient(to bottom, rgba(255, 255, 255, 0.05) 1px, transparent 1px);
            mask-image: linear-gradient(to bottom, black 40%, transparent 100%);
        }

        /* Mobile Menu */
        #mobile-menu {
            transform: translateY(-100%);
            transition: transform 0.6s cubic-bezier(0.22, 1, 0.36, 1);
        }
        #mobile-menu.open { transform: translateY(0); }
    </style>
</head>
<body class="antialiased selection:bg-aura-gold selection:text-black">

    <!-- NOISE OVERLAY (Cinematic Texture) -->
    <div class="noise-overlay"></div>

    <!-- CUSTOM CURSOR -->
    <div class="cursor-dot"></div>
    <div class="cursor-circle"></div>

    <!-- SCROLL PROGRESS -->
    <div id="scroll-progress"></div>

    <!-- AUDIO ASSETS (Short UI Sounds) -->
    <audio id="snd-hover" src="data:audio/wav;base64,UklGRl9vT1BXQVZFZm10IBAAAAABAAEAQB8AAEAfAAABAAgAZGF0YU..."></audio> 
    <audio id="snd-click" src="data:audio/wav;base64,UklGRl9vT1BXQVZFZm10IBAAAAABAAEAQB8AAEAfAAABAAgAZGF0YU..."></audio>

    <!-- PRELOADER -->
    <div id="preloader">
        <div class="font-serif italic text-4xl text-white mb-2">Aura<span class="text-aura-gold">.</span></div>
        <div class="font-mono text-xs text-aura-gold" id="percent">0%</div>
        <div class="loader-bar"><div class="loader-progress" id="bar"></div></div>
    </div>

    <!-- HEADER -->
    <header class="fixed w-full z-50 py-4 px-6 md:px-12 flex justify-between items-center mix-blend-difference">
        <a href="#" onclick="navigateTo('home'); playClick()" class="font-serif italic text-2xl md:text-3xl font-bold tracking-tighter text-white z-50 hover:opacity-80 transition relative sfx-hover">
            Aura<span class="text-aura-gold">.</span>
        </a>

        <nav class="hidden md:flex gap-10">
            <button onclick="navigateTo('philosophy'); playClick()" class="text-[11px] uppercase tracking-[0.2em] hover:text-aura-gold transition-colors text-white/80 sfx-hover">Philosophy</button>
            <button onclick="navigateTo('expertise'); playClick()" class="text-[11px] uppercase tracking-[0.2em] hover:text-aura-gold transition-colors text-white/80 sfx-hover">Expertise</button>
            <button onclick="navigateTo('vision'); playClick()" class="text-[11px] uppercase tracking-[0.2em] hover:text-aura-gold transition-colors text-white/80 sfx-hover">Vision</button>
        </nav>

        <div class="flex items-center gap-6">
            <button onclick="navigateTo('portal'); playClick()" class="hidden md:block px-6 py-2 border border-white/20 rounded-full text-[10px] uppercase tracking-widest hover:bg-white hover:text-black transition-all duration-500 group overflow-hidden relative sfx-hover">
                <span class="relative z-10">Client Portal</span>
                <div class="absolute inset-0 bg-white transform translate-y-full group-hover:translate-y-0 transition-transform duration-300 ease-out z-0"></div>
            </button>
            <button id="menu-btn" class="md:hidden text-white z-50 relative p-2 sfx-hover">
                <i data-lucide="menu" class="w-6 h-6"></i>
            </button>
        </div>
    </header>

    <!-- MOBILE MENU -->
    <div id="mobile-menu" class="fixed inset-0 z-40 bg-black/95 backdrop-blur-xl flex flex-col items-center justify-center space-y-8">
        <button onclick="mobileNavigate('home')" class="text-4xl font-serif text-white hover:text-aura-gold transition">Home</button>
        <button onclick="mobileNavigate('philosophy')" class="text-4xl font-serif text-white hover:text-aura-gold transition">Philosophy</button>
        <button onclick="mobileNavigate('expertise')" class="text-4xl font-serif text-white hover:text-aura-gold transition">Expertise</button>
        <button onclick="mobileNavigate('vision')" class="text-4xl font-serif text-white hover:text-aura-gold transition">Vision</button>
        <button onclick="mobileNavigate('portal')" class="px-8 py-3 mt-8 border border-white/20 rounded-full text-sm uppercase tracking-widest text-white">Client Portal</button>
    </div>

    <main id="app-container" class="relative">

        <!-- ================= HOME PAGE ================= -->
        <div id="home" class="page-container active">

            <!-- Hero with Three.js Background -->
            <section class="relative h-screen w-full flex flex-col justify-center px-6 md:px-12 overflow-hidden">
                <canvas id="webgl-canvas"></canvas> <!-- 3D Particle Field -->

                <div class="relative z-10 mt-12 md:mt-0 pointer-events-none">
                    <div class="overflow-hidden">
                        <h1 class="text-huge font-serif text-white split-text">
                            Wealth <br>
                            <span class="text-aura-gold italic pl-4 md:pl-20">Beyond</span> <br>
                            Horizon
                        </h1>
                    </div>

                    <div class="flex flex-col md:flex-row items-start md:items-end justify-between mt-12 border-t border-aura-border pt-6 max-w-7xl pointer-events-auto">
                        <p class="w-full md:max-w-md text-aura-gray text-base leading-relaxed fade-up">
                            We engineer financial resilience using nano-second algorithmic precision and deep-tier global asset allocation.
                        </p>

                        <!-- Floating Button -->
                        <div class="flex gap-4 mt-8 md:mt-0 fade-up delay-100 cursor-pointer group items-center sfx-hover" onclick="navigateTo('philosophy'); playClick()">
                            <div class="w-8 h-8 rounded-full border border-white/30 flex items-center justify-center group-hover:bg-aura-gold group-hover:border-aura-gold group-hover:text-black transition-all duration-500 animate-[bounce_3s_infinite]">
                                <i data-lucide="arrow-down" class="w-3 h-3"></i>
                            </div>
                            <span class="text-[10px] uppercase tracking-[0.2em] self-center group-hover:text-aura-gold transition-colors duration-300">Explore Strategy</span>
                        </div>
                    </div>
                </div>
            </section>

            <!-- Slim Ticker -->
            <section class="py-3 bg-aura-gold text-black overflow-hidden border-y border-black z-20 relative">
                <div class="marquee-wrap">
                    <div class="marquee-item flex gap-12 items-center text-sm font-bold uppercase tracking-widest px-6">
                        <span>Algorithmic Trading</span> <span>•</span> <span>Private Equity</span> <span>•</span> <span>Global Macro</span> <span>•</span> <span>Nano-Tech Infrastructure</span> <span>•</span> 
                        <span>Algorithmic Trading</span> <span>•</span> <span>Private Equity</span> <span>•</span> <span>Global Macro</span> <span>•</span> <span>Nano-Tech Infrastructure</span> <span>•</span>
                    </div>
                    <div class="marquee-item flex gap-12 items-center text-sm font-bold uppercase tracking-widest px-6">
                        <span>Algorithmic Trading</span> <span>•</span> <span>Private Equity</span> <span>•</span> <span>Global Macro</span> <span>•</span> <span>Nano-Tech Infrastructure</span> <span>•</span> 
                        <span>Algorithmic Trading</span> <span>•</span> <span>Private Equity</span> <span>•</span> <span>Global Macro</span> <span>•</span> <span>Nano-Tech Infrastructure</span> <span>•</span>
                    </div>
                </div>
            </section>

            <!-- Intro Section -->
            <section class="py-24 px-6 md:px-12 bg-aura-black relative z-10">
                <div class="max-w-7xl mx-auto flex flex-col md:flex-row gap-16 md:gap-32">
                    <div class="w-full md:w-1/3 fade-up">
                        <span class="text-aura-gold text-[10px] font-bold tracking-[0.2em] uppercase block mb-4">The Asymmetry</span>
                        <h2 class="text-4xl md:text-5xl font-serif leading-tight text-white">
                            Information is <br> <span class="italic text-gray-500">Currency.</span>
                        </h2>
                    </div>
                    <div class="w-full md:w-2/3 space-y-8 fade-up delay-100">
                        <p class="text-xl text-white font-light leading-relaxed">
                            By the time you read the news, the alpha has already been extracted by machines.
                        </p>
                        <p class="text-aura-gray leading-relaxed text-sm">
                            Aura processes 40TB of global financial data daily—satellite imagery, sentiment analysis, and real-time liquidity pools—to make decisions before the market wakes up.
                        </p>
                    </div>
                </div>
            </section>

             <!-- REDESIGNED CTA SECTION -->
             <section class="relative min-h-[80vh] flex flex-col justify-center items-center text-center px-6 overflow-hidden border-t border-white/5">
                 <!-- Background Elements -->
                 <div class="absolute inset-0 bg-[radial-gradient(circle_at_center,_var(--tw-gradient-stops))] from-aura-gold/10 via-black to-black opacity-50"></div>
                 <div class="absolute inset-0 grid-bg opacity-30 transform perspective-3d rotate-x-60"></div>

                 <div class="relative z-10 max-w-4xl mx-auto space-y-8">
                     <div class="relative">
                        <!-- Huge Background Text -->
                        <h2 class="text-[12vw] md:text-[15rem] leading-none font-serif text-outline opacity-20 select-none absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 w-full">ASCEND</h2>

                        <!-- Foreground Content -->
                        <span class="text-aura-gold text-xs font-bold tracking-[0.3em] uppercase block mb-6">Membership</span>
                        <h2 class="text-5xl md:text-8xl font-serif text-white relative">
                            Join the <br> <span class="italic text-aura-gold">Inner Circle</span>
                        </h2>
                     </div>

                     <p class="text-aura-gray max-w-lg mx-auto text-sm leading-loose pt-4">
                         Access is limited to qualified purchasers. Applications are vetted by our internal committee on a quarterly basis.
                     </p>

                     <button onclick="navigateTo('portal')" class="group relative px-12 py-5 bg-transparent border border-white/20 rounded-full font-bold text-xs uppercase tracking-widest overflow-hidden sfx-hover transition-all duration-300 hover:border-aura-gold hover:shadow-[0_0_30px_rgba(212,175,55,0.2)]">
                         <span class="relative z-10 text-white group-hover:text-black transition-colors duration-300">Request Invitation</span>
                         <div class="absolute inset-0 bg-aura-gold transform translate-y-full group-hover:translate-y-0 transition-transform duration-300 ease-out z-0"></div>
                     </button>
                 </div>
             </section>
        </div>

        <!-- ================= PHILOSOPHY PAGE ================= -->
        <div id="philosophy" class="page-container">
            <section class="pt-32 px-6 md:px-12 min-h-screen">
                <div class="max-w-7xl mx-auto">
                    <h2 class="text-5xl md:text-8xl font-serif mb-16 leading-none">The Nano <br> <span class="italic text-gray-500">Difference.</span></h2>
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-16">
                         <div class="h-[500px] w-full bg-gray-900 overflow-hidden relative group">
                            <div class="absolute inset-0 bg-[url('https://images.unsplash.com/photo-1639322537231-2f206e06af84?q=80&w=2832')] bg-cover bg-center opacity-40 group-hover:scale-105 transition-transform duration-700"></div>
                        </div>
                        <div class="flex flex-col justify-center space-y-6">
                            <h3 class="text-2xl font-light text-white">We don't predict. We calculate.</h3>
                            <p class="text-aura-gray text-sm leading-relaxed">Our proprietary rendering engines visualize market liquidity pools in real-time, executing capital movements 0.02ms before a market trend becomes visible.</p>
                        </div>
                    </div>
                </div>
            </section>
        </div>

        <!-- ================= EXPERTISE PAGE ================= -->
        <div id="expertise" class="page-container">
            <section class="pt-32 px-6 md:px-12 bg-aura-dark min-h-screen">
                <h2 class="text-5xl md:text-7xl font-serif mb-16">Core <span class="italic text-aura-gold">Expertise</span></h2>
                <div class="grid grid-cols-1 gap-4">
                    <!-- Item 1 -->
                    <div class="group border-t border-aura-border py-12 flex flex-col md:flex-row justify-between items-start hover:bg-white/5 transition-colors px-4 cursor-pointer sfx-hover">
                        <div class="text-aura-gold text-xs font-bold tracking-widest uppercase mb-2">01</div>
                        <h3 class="text-4xl font-serif text-white group-hover:translate-x-4 transition-transform">Venture & Growth</h3>
                        <p class="text-gray-500 text-sm max-w-xs mt-4 md:mt-0">Pre-IPO access to disruptive technologies in AI and Biotech.</p>
                    </div>
                    <!-- Item 2 -->
                    <div class="group border-t border-aura-border py-12 flex flex-col md:flex-row justify-between items-start hover:bg-white/5 transition-colors px-4 cursor-pointer sfx-hover">
                        <div class="text-aura-gold text-xs font-bold tracking-widest uppercase mb-2">02</div>
                        <h3 class="text-4xl font-serif text-white group-hover:translate-x-4 transition-transform">Digital Assets</h3>
                        <p class="text-gray-500 text-sm max-w-xs mt-4 md:mt-0">Institutional cold storage and yield generation.</p>
                    </div>
                </div>
            </section>
        </div>

        <!-- ================= VISION PAGE ================= -->
        <div id="vision" class="page-container">
            <section class="pt-32 px-6 md:px-12 min-h-screen bg-[#020202]">
                <div class="max-w-4xl mx-auto text-center mb-24">
                    <h2 class="text-5xl md:text-7xl font-serif mb-8">The <span class="text-aura-gold">2030</span> Roadmap</h2>
                </div>
                <div class="relative max-w-3xl mx-auto border-l border-aura-border pl-12 space-y-20 pb-24">
                    <div class="relative">
                        <div class="absolute -left-[53px] top-1 w-2 h-2 bg-aura-gold rounded-full shadow-[0_0_15px_#D4AF37]"></div>
                        <div class="text-aura-gold font-mono text-xs mb-2">Q4 2024</div>
                        <h3 class="text-2xl font-serif mb-2">Quantum Integration</h3>
                        <p class="text-gray-500 text-sm">Deploying Quantum-Resistant Ledger (QRL).</p>
                    </div>
                    <div class="relative opacity-50">
                        <div class="absolute -left-[53px] top-1 w-2 h-2 bg-gray-700 rounded-full"></div>
                        <div class="text-gray-500 font-mono text-xs mb-2">2026</div>
                        <h3 class="text-2xl font-serif mb-2">Energy Backed Assets</h3>
                    </div>
                </div>
            </section>
        </div>

        <!-- ================= PORTAL (WEB3 LOGIN) ================= -->
        <div id="portal" class="page-container">
            <section class="h-screen w-full flex items-center justify-center px-6 relative overflow-hidden bg-black">
                <div class="glass-panel relative z-10 w-full max-w-sm p-8 bg-[#0a0a0a] border border-white/10 rounded-xl shadow-2xl">
                    <div class="text-center mb-8">
                        <div class="font-serif italic text-3xl font-bold tracking-tighter text-white mb-2">
                            Aura<span class="text-aura-gold">.</span>
                        </div>
                        <p class="text-[10px] uppercase tracking-widest text-gray-500">Institutional Access</p>
                    </div>

                    <div class="space-y-4">
                        <!-- Updated Connect Text -->
                        <button class="w-full bg-white text-black font-bold py-3 rounded-lg hover:bg-gray-200 transition-colors flex items-center justify-center gap-3 sfx-hover">
                            <div class="w-4 h-4 bg-orange-500 rounded-full"></div> 
                            <span class="text-xs uppercase tracking-wider">Connect Wallet</span>
                        </button>
                        <button class="w-full bg-transparent border border-white/20 text-white font-bold py-3 rounded-lg hover:bg-white/5 transition-colors flex items-center justify-center gap-3 sfx-hover">
                            <div class="w-4 h-4 bg-white rounded-full"></div> 
                            <span class="text-xs uppercase tracking-wider">Connect Ledger</span>
                        </button>
                    </div>

                    <div class="my-6 flex items-center gap-4">
                        <div class="h-[1px] bg-white/10 flex-1"></div>
                        <span class="text-[10px] text-gray-600 uppercase">Or</span>
                        <div class="h-[1px] bg-white/10 flex-1"></div>
                    </div>

                    <form onsubmit="event.preventDefault();" class="space-y-4">
                        <input type="text" class="w-full bg-black border border-white/10 rounded-lg px-4 py-3 text-sm text-white focus:border-aura-gold focus:outline-none transition-colors" placeholder="Client ID">
                        <button class="w-full bg-aura-gold text-black font-bold py-3 rounded-lg hover:bg-white transition-colors duration-300 text-xs uppercase tracking-widest sfx-hover">
                            Authenticate
                        </button>
                    </form>
                </div>
            </section>
        </div>

        <!-- LEGAL FOOTER -->
        <footer class="py-16 px-6 md:px-12 bg-black border-t border-white/10 text-xs text-gray-600">
            <div class="max-w-7xl mx-auto grid grid-cols-1 md:grid-cols-4 gap-12">
                <div class="md:col-span-2 space-y-4">
                    <div class="text-white font-serif italic text-xl">Aura.</div>
                    <p class="max-w-md leading-relaxed">
                        Aura Wealth Management is a registered investment adviser. Information presented is for educational purposes only.
                    </p>
                </div>
                <div>
                    <h4 class="text-white uppercase tracking-widest mb-4 font-bold text-[10px]">Locations</h4>
                    <ul class="space-y-2">
                        <li>New York, NY</li>
                        <li>London, UK</li>
                        <li>Singapore, SG</li>
                    </ul>
                </div>
                <div>
                    <h4 class="text-white uppercase tracking-widest mb-4 font-bold text-[10px]">Compliance</h4>
                    <ul class="space-y-2">
                        <li><a href="#" class="hover:text-aura-gold">Terms of Use</a></li>
                        <li><a href="#" class="hover:text-aura-gold">Privacy Policy</a></li>
                    </ul>
                </div>
            </div>
            <div class="max-w-7xl mx-auto mt-12 pt-8 border-t border-white/5 flex flex-col md:flex-row justify-between">
                <p>&copy; 2024 Aura Wealth Management LLC. All rights reserved.</p>
                <p>Regulated by the SEC and FCA.</p>
            </div>
        </footer>

    </main>

    <script>
        // --- 1. PRELOADER LOGIC ---
        const preloader = document.getElementById('preloader');
        const percent = document.getElementById('percent');
        const bar = document.getElementById('bar');

        let load = 0;
        const interval = setInterval(() => {
            load += Math.random() * 5;
            if(load > 100) load = 100;

            percent.innerText = Math.floor(load) + '%';
            bar.style.width = load + '%';

            if(load === 100) {
                clearInterval(interval);
                gsap.to(preloader, {
                    opacity: 0,
                    duration: 1,
                    delay: 0.5,
                    pointerEvents: 'none',
                    onComplete: () => {
                        initHeroAnimations();
                        initThreeJS(); 
                    }
                });
            }
        }, 50);

        // --- 2. CUSTOM CURSOR & SCROLL PROGRESS ---
        const cursorDot = document.querySelector('.cursor-dot');
        const cursorCircle = document.querySelector('.cursor-circle');
        const scrollProgress = document.getElementById('scroll-progress');

        window.addEventListener('mousemove', (e) => {
            const posX = e.clientX;
            const posY = e.clientY;

            // Dot follows instantly
            cursorDot.style.left = `${posX}px`;
            cursorDot.style.top = `${posY}px`;

            // Circle follows with delay
            cursorCircle.animate({
                left: `${posX}px`,
                top: `${posY}px`
            }, { duration: 500, fill: "forwards" });
        });

        // Hover Effect
        document.querySelectorAll('a, button, .sfx-hover').forEach(el => {
            el.addEventListener('mouseenter', () => document.body.classList.add('hovering'));
            el.addEventListener('mouseleave', () => document.body.classList.remove('hovering'));
        });

        // Scroll Progress
        window.addEventListener('scroll', () => {
            const scrollTop = document.documentElement.scrollTop || document.body.scrollTop;
            const scrollHeight = document.documentElement.scrollHeight - document.documentElement.clientHeight;
            const scrolled = (scrollTop / scrollHeight) * 100;
            scrollProgress.style.width = scrolled + '%';
        });


        // --- 3. THREE.JS BACKGROUND ---
        function initThreeJS() {
            const canvas = document.getElementById('webgl-canvas');
            const scene = new THREE.Scene();
            scene.fog = new THREE.FogExp2(0x050505, 0.002);

            const camera = new THREE.PerspectiveCamera(75, window.innerWidth / window.innerHeight, 0.1, 1000);
            camera.position.z = 5;

            const renderer = new THREE.WebGLRenderer({ canvas: canvas, alpha: true, antialias: true });
            renderer.setSize(window.innerWidth, window.innerHeight);
            renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));

            const geometry = new THREE.BufferGeometry();
            const count = 1500;
            const positions = new Float32Array(count * 3);
            const colors = new Float32Array(count * 3);

            for(let i = 0; i < count * 3; i++) {
                positions[i] = (Math.random() - 0.5) * 15;
                colors[i] = Math.random();
            }

            geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3));
            geometry.setAttribute('color', new THREE.BufferAttribute(colors, 3));

            const material = new THREE.PointsMaterial({
                size: 0.03,
                color: 0xD4AF37,
                transparent: true,
                opacity: 0.8,
                blending: THREE.AdditiveBlending
            });

            const particles = new THREE.Points(geometry, material);
            scene.add(particles);

            let mouseX = 0;
            let mouseY = 0;
            let timeout;
            window.addEventListener('mousemove', (e) => {
                if (timeout) cancelAnimationFrame(timeout);
                timeout = requestAnimationFrame(() => {
                   mouseX = (e.clientX - window.innerWidth / 2) * 0.001;
                   mouseY = (e.clientY - window.innerHeight / 2) * 0.001;
                });
            });

            const clock = new THREE.Clock();

            function animate() {
                const elapsedTime = clock.getElapsedTime();
                particles.rotation.y = elapsedTime * 0.05;
                particles.rotation.x = mouseY * 0.5;

                const positions = particles.geometry.attributes.position.array;
                for(let i = 0; i < count; i++) {
                    const i3 = i * 3;
                    positions[i3 + 1] += Math.sin(elapsedTime + positions[i3]) * 0.002; 
                }
                particles.geometry.attributes.position.needsUpdate = true;

                renderer.render(scene, camera);
                requestAnimationFrame(animate);
            }

            animate();

            window.addEventListener('resize', () => {
                camera.aspect = window.innerWidth / window.innerHeight;
                camera.updateProjectionMatrix();
                renderer.setSize(window.innerWidth, window.innerHeight);
            });
        }

        // --- 4. SOUND DESIGN ---
        const audioCtx = new (window.AudioContext || window.webkitAudioContext)();

        function playClick() {
            if (audioCtx.state === 'suspended') audioCtx.resume();

            const oscillator = audioCtx.createOscillator();
            const gainNode = audioCtx.createGain();

            oscillator.type = 'sine';
            oscillator.frequency.setValueAtTime(800, audioCtx.currentTime);
            oscillator.frequency.exponentialRampToValueAtTime(300, audioCtx.currentTime + 0.1);

            gainNode.gain.setValueAtTime(0.1, audioCtx.currentTime);
            gainNode.gain.exponentialRampToValueAtTime(0.01, audioCtx.currentTime + 0.1);

            oscillator.connect(gainNode);
            gainNode.connect(audioCtx.destination);

            oscillator.start();
            oscillator.stop(audioCtx.currentTime + 0.1);
        }

        // --- 5. NAVIGATION ---
        document.addEventListener("DOMContentLoaded", () => {
             if (typeof lucide !== 'undefined') lucide.createIcons();
        });

        let lenis;
        if (typeof Lenis !== 'undefined') {
            lenis = new Lenis({ duration: 1.2, smooth: true });
            function raf(time) { lenis.raf(time); requestAnimationFrame(raf); }
            requestAnimationFrame(raf);
        }

        const menuBtn = document.getElementById('menu-btn');
        const mobileMenu = document.getElementById('mobile-menu');
        let isMenuOpen = false;

        menuBtn.addEventListener('click', () => {
            isMenuOpen = !isMenuOpen;
            if(isMenuOpen) {
                mobileMenu.classList.add('open');
                menuBtn.innerHTML = '<i data-lucide="x" class="w-6 h-6"></i>';
            } else {
                mobileMenu.classList.remove('open');
                menuBtn.innerHTML = '<i data-lucide="menu" class="w-6 h-6"></i>';
            }
            if(typeof lucide !== 'undefined') lucide.createIcons();
        });

        function mobileNavigate(pageId) {
            mobileMenu.classList.remove('open');
            isMenuOpen = false;
            menuBtn.innerHTML = '<i data-lucide="menu" class="w-6 h-6"></i>';
            if(typeof lucide !== 'undefined') lucide.createIcons();
            setTimeout(() => navigateTo(pageId), 300);
        }

        const pages = ['home', 'philosophy', 'expertise', 'vision', 'portal'];

        function navigateTo(pageId) {
            if(lenis) lenis.scrollTo(0, {immediate: true}); else window.scrollTo(0,0);

            pages.forEach(p => document.getElementById(p).classList.remove('active'));

            setTimeout(() => {
                document.getElementById(pageId).classList.add('active');
                initHeroAnimations();
                ScrollTrigger.refresh();
            }, 100);
        }

        // GSAP Animations
        gsap.registerPlugin(ScrollTrigger);

        function initHeroAnimations() {
            gsap.fromTo('.split-text', 
                { y: 100, opacity: 0 },
                { y: 0, opacity: 1, duration: 1.5, stagger: 0.1, ease: "power4.out" }
            );

            gsap.utils.toArray('.fade-up').forEach(element => {
                gsap.fromTo(element, 
                    { y: 50, opacity: 0 },
                    {
                        scrollTrigger: { trigger: element, start: "top 90%" },
                        y: 0, opacity: 1, duration: 1, ease: "power3.out"
                    }
                );
            });
        }

    </script>
</body>
</html>